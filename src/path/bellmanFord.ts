/**
 * bellman-ford algorithm is suitable for graph with negative cycles
 * vertice can be categoried into three kinds:
 * 1. Unaffected node
 * 2. Directly in negative cycles
 * 3. Reachable by negative cycles 
 */